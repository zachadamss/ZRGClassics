{
  "name": "zrg-classics",
  "version": "1.0.0",
  "description": "Resource Hub for Classic Porsche & BMW",
  "main": ".eleventy.js",
  "scripts": {
    "build": "eleventy",
    "build:search": "node build-search-index.js",
    "build:all": "node build-search-index.js && eleventy",
    "serve": "eleventy --serve",
    "serve:local": "npm run build && serve _site",
    "watch": "eleventy --watch",
    "minify:css": "cleancss -o _site/styles.css _site/styles.css",
    "minify:js": "for f in _site/script.js _site/search.js _site/tools/*.js _site/js/*.js _site/js/pages/*.js; do terser \"$f\" -o \"$f\" -c -m || exit 1; done",
    "minify": "npm run minify:css && npm run minify:js",
    "build:prod": "npm run build:all && npm run minify"
  },
  "keywords": [
    "porsche",
    "bmw",
    "classic-cars",
    "resources"
  ],
  "author": "ZRG Classics",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "clean-css-cli": "^5.6.3",
    "serve": "^14.2.5",
    "terser": "^5.46.0"
  },
  "dependencies": {
    "@11ty/eleventy-img": "^7.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
